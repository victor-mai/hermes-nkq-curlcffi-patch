diff --git a/gateway/run.py b/gateway/run.py
index 321f9b5ad..6877b9eee 100644
--- a/gateway/run.py
+++ b/gateway/run.py
@@ -12978,6 +12978,21 @@ class GatewayRunner:
             except Exception:
                 pass
 
+        # Keep typing indicator alive during the SSE stream
+        _stop_typing = asyncio.Event()
+
+        async def _keep_typing_loop():
+            while not _stop_typing.is_set():
+                _typing_adapter = self.adapters.get(source.platform)
+                if _typing_adapter:
+                    try:
+                        await _typing_adapter.send_typing(source.chat_id, metadata=_thread_metadata)
+                    except Exception:
+                        pass
+                await asyncio.sleep(2.0)
+
+        _typing_task = asyncio.create_task(_keep_typing_loop())
+
         # Make the HTTP request with SSE streaming -----------------------
         full_response = ""
         _start = time.time()
@@ -13060,6 +13075,14 @@ class GatewayRunner:
                 }
             # Partial response — return what we got
         finally:
+            # Stop typing loop
+            _stop_typing.set()
+            if _typing_task:
+                _typing_task.cancel()
+                try:
+                    await _typing_task
+                except asyncio.CancelledError:
+                    pass
             # Finalize stream consumer
             if _stream_consumer:
                 _stream_consumer.finish()
